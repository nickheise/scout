dialkit: tuning animation springs, easing curves, colors, or layout numbers by feel while the app is running; a plan involves iterating on interface parameters (spring physics, thresholds, sizes) that would otherwise mean edit-and-reload cycles; prototyping motion or visual design where someone needs live knobs over component props without code changes
emilkowalski-skills: auditing a UI codebase for animation and motion-polish opportunities; reviewing or refining existing UI animations — easing, duration, transform and shadow choices; a plan adds motion to components (modals, toasts, menus, page transitions) and needs taste-level rules rather than more code
mattpocock-skills: starting a sizeable feature where requirements should be pinned down by an alignment interview before any code; running a test-driven (red-green-refactor) implementation loop with an agent; producing a handoff document or a codebase-architecture improvement plan
paper-shaders: a plan calls for an animated background, hero-section visual, or gradient effect on a web page; adding a mesh gradient, orbiting-dots, or similar canvas shader effect without writing GLSL; replacing a video file or heavyweight animation library that is only serving as a decorative background
shadcn-ui: building a dialog, dropdown, form, data table, or command menu in a React app; starting a new React or Next.js app that needs a component foundation or design system; a plan calls for accessible UI primitives the project will customize and own rather than take as a locked dependency

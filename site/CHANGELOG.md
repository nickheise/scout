# Changelog — Scout Marketing Site

All notable changes to this project. Format loosely follows [Keep a Changelog](https://keepachangelog.com/); dates are ISO.

## [Unreleased]

### Changed
- 2026-07-28 — Positioning widened; field guide and field notes integrated as first-class features; tracks retired; courier-prompt naming unified (D-020). **The site now describes the intended feature set in present tense and may run ahead of the plugin — Nick holds the production gate.** Two new sections: `FieldGuide.tsx` ("The pack goes in. The field guide comes out.") carrying the D-021 pack/field-guide distinction, the three entry types (Links / Practices / Standards, with D-022's exemplar/rubric/gotcha named inside), and the compiled output; `TasteLoop.tsx` ("It gets more like you, not less.") carrying D-023's taste loop with field notes as a user-facing feature. Hero and meta copy widen from libraries-only to the full triad (what to reach for / how you work / what good looks like); HowItWorks step 2 renamed "The manifest" → "The field guide" (the manifest is one of three compiled outputs) and step 3 now reflects work-phase surfacing. `TracksPrompt.tsx` → `CourierPrompt.tsx` (unifies with core's only name for the artifact, `docs/courier-prompt.md`); the placeholder prompt text is replaced with the real, shipped courier prompt; the eyebrow changes from "Tracks" to functional "Before you install"; Philosophy's closing line drops "reads tracks, not answers" for "reads your history, not your answers," and its fine print now discloses field notes against the no-telemetry claim. Install command mirror updated to `scout@nickheise` (synced from core D-024). A first attempt at this — a "Designed, not shipped" `Roadmap.tsx` appendix — was built and rejected on review as quarantine rather than integration; PRD §11's Parked ideas (expedition patches, supersession-as-centerpiece) were dropped from the site with it.

### Fixed
- 2026-07-28 — Missing space after inline `<code>` elements in HowItWorks, which rendered `/scout:adda URL` and `/scout:starton a new project` (pre-existing, carried over from the Astro build). Fixed with the codebase's explicit `{" "}` separator convention, as already used in `Install.tsx`. Verified in the DOM: zero occurrences of `/scout:<verb>` followed immediately by a letter across all 53 text blocks on the page.

### Added
- 2026-07-20 — Icon sprite extended per `docs/visual-language.md` §4 (D-019): three new monoline glyphs (`blaze`, `tracks`, `cairn`) added to `IconSprite.astro`/`Icon.astro` on the existing 24px/1.75px-stroke convention. Fixed the `pack` glyph's padlock-at-small-sizes problem — the single shoulder-strap arc bridging the body (which read as a shackle) is now two separate strap curves, and the ambiguous inner lines resolved into a proper front-pocket rect. Applied the `tracks` glyph beside the stated/revealed-preferences blockquote in `Philosophy.astro` (D-019 glyph map: tracks = revealed preferences). Evaluated swapping the HowItWorks "Pack it" step icon — kept `pack` (now fixed): it already maps to "the category itself" per the glyph table, and blaze is reserved for reports. No `--color-report` usage outside the demo, no new animation. (Landed via handoff from a sandboxed worktree session that couldn't write outside `scout-marketing`; verified byte-identical against its source before copying in.)
- 2026-07-20 — Visual language reference merged (`docs/visual-language.md`, D-019): iconography families with glyph↔concept mappings, palette semantics (blaze↔report color), avoid list (fleur-de-lis, surveillance glyphs, tactical register, merit-badge framing, mascots), reference sites, motion rules — consolidated from the July 2026 design research and iconography survey. Sprite additions (blaze/tracks/cairn) and pack-glyph fix queued for the v1 polish pass.
- 2026-07-20 — Beat 2 reconciled against the real product (D-011): engineering wrapped all five Scout build phases (plugin v0.4.0) and sent live output captures (`docs/real-output-captures.md`). Demo script v2: Act 2 report is now the real compressed card (Matched clause quotes Act 1's packs-when line); no no-op scene per Nick; Beat 3 "one-line reports" → "compact reports". Craft pass from the how-to reference page landed in `d676415` (interactions, masked-wash topo texture, demo lifecycle discipline); standalone Opus review of that commit in flight after the workflow's reviewer stalled.
- 2026-07-19 — Brand positioning pass (Dunford, `docs/scout-positioning.md`): category anchored as memory-for-coding-agents / taste-not-transcripts subsegment, Big Fish Small Pond (D-018). Applied copy-level: two new copy-deck guardrails (no team-policy marketing, no recommendations marketing), restraint stat ("capped at 25 lines — smaller than most CLAUDE.md files") in deck + Philosophy section, category-anchored `siteDescription` in config, canonical comparison-surface strings (README line, directory description, HN title, "Isn't this just…?" FAQ) added to the deck as Scout-core handoff. Launch post drafted on the insight spine (`docs/launch-post.md`). Hero-test guidance recorded: A vs C, retire B.
- 2026-07-19 — Site PRD v0.3 synced (`docs/prd-scout-marketing-site-v0.3.md`): `/scout:review` renamed `/scout:survey` (verb slate locked) — Beat 3 updated in section + copy deck; "plain in, brand out" register doctrine and the tenet-7 never-market-retrieval guardrail recorded in the copy deck for the v1 copywriting pass (D-017).
- 2026-07-19 — Install strings CONFIRMED by engineering (plugin v0.1.0, Phase 2 shipped) — identical to what the site already renders, so zero copy change. Two launch gates recorded in D-004: the scout repo is local-only (site links 404 until it's public) and `/scout:review` + `/scout:setup` land with Phases 3–4 while the site's copy references both. Router template exists in the scout repo for the D-016 callout link.
- 2026-07-19 — Engineering sync absorbed: name stays Scout (Recon declined), repo home github.com/nickheise/scout, provisional two-step plugin install wired into config (real org/repo replace placeholders; only domain still deferred), CopyBlock auto-detects multiline commands and left-aligns them, router-skill callout recorded as D-016 for the v1 copy pass.
- 2026-07-19 — **v0 gate reached.** Full six-beat site built and verified. Agent-team build completed (scaffold `6d9d967` → design system `7af5862` → six beats + demo island `9a77ee5`); Opus review verdict "fix-then-ship" — all 7 findings fixed plus P4 (gated GoatCounter, build-time Satori OG card, CSP meta, head hygiene) in `c653108`; orchestrator visual-QA + verification fixes (footer credit whitespace, plugin-shaped install placeholder, `<main>` landmark) in `2d4b417`. **Measured verification: Lighthouse mobile 96/98/100/100, desktop 100/98/100/100 (pre-landmark-fix accessibility; landmark now fixed), 288 KiB cold-load transfer vs 1.5 MB budget, JS strictly the three islands.** Outstanding for v1: real tracks prompt (Scout core), Nick's example-pack entries, name swap (D-004), GoatCounter site code, barbecue test, OG unfurl check in real clients, monorepo `/site` lift.
- 2026-07-18 — Project kickoff. Imported PRD v0.2 (`docs/prd-scout-marketing-site-v0.2.md`), created decision log (`DECISIONS.md`) and this changelog. Research spun up on stack (Astro/styling/perf/OG/hosting) and demo craft (terminal animation techniques, reference sites, type pairings). Build blocked pending resolution of PRD §9 open questions — see `DECISIONS.md`.
- 2026-07-18 — Research reports delivered (stack + demo craft). Decisions locked with Nick: Astro 7 static (D-001), Tailwind v4 + shadcn-in-islands (D-002), hand-built demo animation (D-003), MIT (D-005), problem-led hero default (D-006), v1-staged-through-v0 scope (D-007), Vercel hosting (D-008), GoatCounter analytics (D-009). Domain/org availability checked: `scout-pack`, `packscout`, `scoutpack` free as both .dev domain and GitHub org (D-004). Build plan written (`PLAN.md`). Still blocking: name choice, tracks-prompt authorship, demo content source, type sign-off, repo placement — see PLAN §6.
- 2026-07-18 — Final blockers resolved with Nick: name deferred to pre-Phase-2 (config constant meanwhile) (D-004), demo is an art-directed recreation (D-011), tracks prompt owned by Scout core — site ships placeholder block (D-010), typography via shadcn Typeset with field-guide fonts through its variables (D-012), standalone repo with planned `/site` monorepo lift (D-013). Copy deck and demo script drafted (`docs/copy-deck.md`, `docs/demo-script.md`). Orchestrated build started: git initialized, agent team dispatched (scaffold → design system → parallel beats + demo island → Opus review).
- 2026-07-18 — Naming sync from Scout engineering (their D-007): repo/plugin locked as `scout`, commands are colon-namespaced (`/scout:add`). Copy deck + demo script updated to real command form (D-014); built sections to be verified in post-review triage. Marketing domain evaluation delivered: recommend `scoutpack.dev` over getscout/heyscout/scoutcc (D-015); reported to engineering thread, Nick to decide.
